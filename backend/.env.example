# Local dev (SQLite — comment this out for PostgreSQL)
# DATABASE_URL="file:./dev.db"

# Production / Railway (PostgreSQL — uncomment and set your connection string)
# DATABASE_URL="postgresql://user:password@host:5432/seapedia"

JWT_SECRET="change-this-to-a-random-secret"
JWT_EXPIRES_IN="24h"
PORT=3001
